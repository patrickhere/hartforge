---
ansible_ssh_private_key_file: ~/.ssh/id_ed25519
ansible_python_interpreter: /usr/bin/python3
vps_public_ip: 203.0.113.10
svc_name: Authentik
svc_check: "docker ps --filter name=authentik-server --filter status=running -q | grep -q ."
svc_port: 9000

vps_services:
  - authentik
  - uptime-kuma
  - forgejo-mirror
  - ntfy
