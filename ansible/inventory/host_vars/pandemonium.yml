---
svc_name: Pandemonium
svc_check: "docker ps --filter name=pandemonium-broker --filter status=running -q | grep -q ."
svc_port: 8080
compose_path: /opt/pandemonium

backup_files:
  - { src: "/opt/pandemonium/docker-compose.yaml", dest: "pandemonium/docker-compose.yaml" }
